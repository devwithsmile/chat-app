version: '3.9'

networks:
  app-tier:
    driver: bridge

services:
  myapp:
    image: 'chatapp'
    networks:
      - app-tier
    ports:
      - 8000:8000
    links:
      - mongodb  
    depends_on:
      - mongodb

  mongodb:
    image: 'mongo'
    networks:
      - app-tier
    environment:
      - ALLOW_EMPTY_PASSWORD=yes
    ports:
      - 27018:27017